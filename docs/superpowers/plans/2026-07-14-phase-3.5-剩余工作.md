# Phase 3.5 剩余工作清单（基于当前代码状态）

&gt; 状态：待执行
&gt;
&gt; 前置分支：`feat/helm`
&gt;
&gt; 基于：[2026-06-28-helm-hermes-phase3.5-engine-bridge.md](file:///Users/jiaxing/code/github/jadekit/docs/superpowers/plans/2026-06-28-helm-hermes-phase3.5-engine-bridge.md)

---

## 📊 当前代码状态速查

| 原始计划任务 | 现状 | 完成度 |
|------------|------|--------|
| Task 1: worker transcript 桥 | API 已建但返回 `source="none"` 空数组 | ⚠️ 部分完成 |
| Task 2: judge 接通 Coordinator | `hermes_judge_show()` 永远返回 `null` | ⚠️ 部分完成 |
| Task 3: 单-agent abort + 引擎小清理 | ✅ `hermes_agent_abort()` 已完整实现 | ✅ 完成 |
| Task 4: mock 运行命令 | ✅ `hermes_run_mock()` 已完整实现 | ✅ 完成 |

---

## 🔧 剩余待完成任务

### 1. Task 1（续）：worker transcript 桥完全打通

**现状**：
- 表已建：`worker_sessions` + `worker_transcript_entries`（见 [store.rs#L214-L223](file:///Users/jiaxing/code/github/jadekit/src-tauri/src/hermes/store.rs#L214-L223)）
- API 已占位：`hermes_worker_transcript` + `hermes_worker_session_list`（见 [hermes_commands.rs#L737-L760](file:///Users/jiaxing/code/github/jadekit/src-tauri/src/commands/hermes_commands.rs#L737-L760)）
- **缺失**：真实的落库逻辑 + Store CRUD

**方案选择（二选一）**：

| 方案 | 说明 | 推荐 |
|-----|------|------|
| **方案 A：按原始计划，从 session_manager 桥接** | 复用 `session_manager::load_messages` 读取 daemon 落盘的 session | ⚠️（需要确认 daemon 是否回传了 session_id） |
| **方案 B：直接从 SdkRuntime 边发边存** | 在 `sdk_runtime.rs` 的 `send()` 里，每解析到 `AgentEvent` 就写表 | ✅（更简单，当前代码结构更适合） |

**推荐：方案 B**（当前代码里 `SdkRuntime` 已经有完整的事件解析逻辑，直接落库最直接）。

---

#### 执行步骤（方案 B）

##### 1.1 新增 `Store` 的 Transcript CRUD

在 `src-tauri/src/hermes/store.rs` 新增：

```rust
impl Store {
    // --- Worker Session ---
    pub fn create_worker_session(&self, session: WorkerSession) -> Result<(), String> { ... }
    pub fn update_worker_session_final(&self, dispatch_id: &str, final_response: Option<&str>, error: Option<&str>) -> Result<(), String> { ... }
    pub fn list_worker_sessions(&self, run_id: Option<&str>) -> Result<Vec<WorkerSession>, String> { ... }

    // --- Worker Transcript Entry ---
    pub fn append_transcript_entry(&self, entry: WorkerTranscriptEntry) -> Result<(), String> { ... }
    pub fn list_transcript_entries(&self, dispatch_id: &str) -> Result<Vec<WorkerTranscriptEntry>, String> { ... }
}
```

##### 1.2 修改 `SdkRuntime::send()` 边发边存

在 `src-tauri/src/hermes/sdk_runtime.rs`：
1. 把 `Store` 注入到 `SdkRuntime` 里
2. 在 spawn 转发任务里，每解析出一个 `AgentEvent` 就写 `worker_transcript_entries`
3. 在 send 开始时创建 `worker_sessions` 记录，结束时更新终态

##### 1.3 补全 `hermes_commands.rs` 的命令实现

修改 `worker_transcript` 和 `list_worker_sessions` 方法，调用真实 Store API。

---

### 2. Task 2（续）：judge 接通 Coordinator

**现状**：
- `hermes_judge_show(runId)` API 已建但永远返回 `null`（见 [hermes_commands.rs#L729-L735](file:///Users/jiaxing/code/github/jadekit/src-tauri/src/commands/hermes_commands.rs#L729-L735)）
- `Planner::judge()` 已实现
- **缺失**：Coordinator 在收敛/多候选点调用 judge + verdict 落库

**执行步骤**：

1. 在 `Store` 新增 Judge 结果 CRUD（或复用现有表）
2. 修改 `Coordinator`，在合适的时机调用 `Planner::judge()` 并写库
3. 补全 `hermes_judge_show()` 从库读取

---

### 3. 额外：Permission Prompt 解析支持（原始计划里没提但需要）

**现状**：
- `supports_permission_prompt = false`（见 [sdk_runtime.rs#L313-L316](file:///Users/jiaxing/code/github/jadekit/src-tauri/src/hermes/sdk_runtime.rs#L313-L316)）
- **缺失**：从 AI-Bridge 识别权限请求事件并解析成 `NeedsAttention`

**执行步骤**：
1. 研究 `permission-mode.js` + `message-sender.js`，确认权限事件标签
2. 从 `METADATA_LABELS` 移除对应标签（如果在里面）
3. 在 `parse_stream_line()` 新增解析分支，把权限请求解析成对应事件
4. 更新 `RuntimeCapabilities::supports_permission_prompt = true`

---

### 4. 额外：Resume 支持（可选但建议）

**现状**：
- `supports_resume = false`
- **缺失**：agent 断点恢复能力

---

## ✅ 验证与交付标准

### 单元测试
- [ ] 新增任务单测覆盖 &gt;= 80%
- [ ] `cargo test` 全部通过
- [ ] `npm run test:run` 前端测试通过

### 集成测试
- [ ] 完整链路验证：
  1. 启动 Helm Cockpit
  2. 选 repo/选模型
  3. 发送 goal
  4. 观察任务拆解/派发/执行
  5. 验证 Transcript 能展示 agent 完整过程
  6. 验证权限请求能正确弹出 gate
  7. 验证 resolve 后能继续执行

### Lint &amp; Build
- [ ] `npm run lint` 无错误
- [ ] `npm run build` 通过
- [ ] `cargo check` 通过

### 交付物
- [ ] Phase 3.5 功能完整可用
- [ ] 重新开启 Helm 入口（取消 [Sidebar.tsx#L18](file:///Users/jiaxing/code/github/jadekit/src/components/layout/Sidebar.tsx#L18) 的注释）
- [ ] Git 提交历史清晰，有意义

---

## 📌 关键注意事项

1. **不要破坏现有功能**：Phase 0-4 已经完成的功能要保持非回归
2. **保持 API 兼容**：前端调用方式不要变
3. **优先做核心任务**：Worker Transcript &amp; Judge 接通是核心
4. **和原始计划对齐**：尽量保持和 [2026-06-28-helm-hermes-phase3.5-engine-bridge.md](file:///Users/jiaxing/code/github/jadekit/docs/superpowers/plans/2026-06-28-helm-hermes-phase3.5-engine-bridge.md) 的术语一致
5. **做完记得开 Helm 入口**：Phase 3.5 完成后取消注释

---

## 📚 相关文档参考

- [Phase 3.5 原始计划](file:///Users/jiaxing/code/github/jadekit/docs/superpowers/plans/2026-06-28-helm-hermes-phase3.5-engine-bridge.md)
- [helm-hermes-ui-contract.md](file:///Users/jiaxing/code/github/jadekit/docs/helm-hermes-ui-contract.md)
- [Phase 4 Cockpit 计划](file:///Users/jiaxing/code/github/jadekit/docs/superpowers/plans/2026-06-28-helm-hermes-phase4-cockpit.md)
- [Phase 5/6 Backlog](file:///Users/jiaxing/code/github/jadekit/docs/superpowers/plans/2026-06-28-helm-phase5-6-backlog.md)

---

**下一个 Agent，请从这里开始！** 🚀
